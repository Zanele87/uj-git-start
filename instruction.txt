Let’s do this:
• (done) create a local repository called uj-git-start,
• (done) and add two files to it,
• (done) commit the files into repo,
• (done) let’s create a branch called say-hello,
• (done) lets change into the branch
• (done) and create a file in the branch,
• () add and commit to the branch,
• () merge the branch into main.

